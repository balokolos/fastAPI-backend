apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-coupon.fullname" . }}
  labels:
    {{- include "app-coupon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-coupon.selectorLabels" . | nindent 4 }}
