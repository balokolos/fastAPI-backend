apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-coupon.fullname" . }}-test-connection"
  labels:
    {{- include "app-coupon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-coupon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
